	// string a = "National university of computer and emerging sciences";
	// string s;
	// cin >> s;
	// int l1 = a.find(s);
	// int l2 = s.length();
	// sort(s.begin(), s.end());
	// a.replace(l1,l2, s);
	// cout << a;