	// int arr[] = { 1,2,1,4,5 };
	// int length = sizeof(arr) / sizeof(arr[0]);
	// bool True = true;
	// for (int i = 1; i < length-1; i++)
	// {
	// 	for (int j = 0; j < i; j++) {
	// 		if (arr[j] > arr[i]) True = false;
	// 	}
	// 	for (int k = i + 1; k < length; k++) {
	// 		if (arr[i] > arr[k]) True = false;
	// 	}

	// 	if (True == 1) cout << arr[i] << " ";
	// 	True = 1;
	// }