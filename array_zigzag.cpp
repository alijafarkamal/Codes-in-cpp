	// int arr[] = { 4,3,7,8,6,2,1 };
	// int length = sizeof(arr) / sizeof(arr[0]);
	// for (int i = 0; i < length-1; i++)
	// {
	// 	if ((i % 2 == 0 && arr[i] > arr[i + 1]) || (arr[i]<=arr[i+1] && i%2==1))
	// 	{
	// 		int temp = arr[i];
	// 		arr[i] = arr[i + 1];
	// 		arr[i + 1] = temp;
	// 	}
		
	// }
	// for (int i = 0; i < length; i++) {
	// 	cout << arr[i] << " ";
	// }



    // 	int arr[] = { 4,3,7,8,6,2,1 };
	// int length = sizeof(arr) / sizeof(arr[0]);
	// for (int i = 0; i < length - 1; i++)
	// {
	// 	if (i % 2 == 0) {
	// 		if (arr[i] < arr[i + 1]) i;
	// 		else {
	// 			int temp = arr[i];
	// 			arr[i] = arr[i + 1];
	// 			arr[i + 1] = temp;
	// 		}
	// 	}
	// 	else {
	// 		if (arr[i] > arr[i + 1]) i;
	// 		else {
	// 			int temp = arr[i];
	// 			arr[i] = arr[i + 1];
	// 			arr[i + 1] = temp;
	// 		}

	// 	}
	// }
	// for (int i = 0; i < length; i++) {
	// 	cout << arr[i] << " ";
	// }