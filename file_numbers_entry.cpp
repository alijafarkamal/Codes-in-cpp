	// const int n = 10;
	// int num[n];
	// ifstream in("input.txt");
	// if (!in) cout << "Error while opening the file\n";
	// for (int i = 0; i < n; i++)
	// 	in >> num[i];
	// in.close();
	// ofstream  out("output.txt");
	// for (int i = 0; i < n; i += 2)
	// 	out << num[i];
	// for (int i = 1; i < n; i += 2)
	// 	out << num[i];
	// out.close();