    // for (int i = str.length(); str[i] != ' '; i--) {
    //     count++;
    // }
    // bool True = true;
    // int j = 0;

    // for(int i = str.length()-count+1; str[i]!= '\0'; i++)
    //     if (str[i] != str1[j++]) {
    //         True = false;
    //      }
    
    // if (True) cout << "Matched";
    // else cout << "Not matched";